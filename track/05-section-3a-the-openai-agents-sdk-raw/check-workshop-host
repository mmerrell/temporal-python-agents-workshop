#!/bin/bash
set -euo pipefail

grep -q "@function_tool" /workspace/exercise/agent_raw.py \
    || { fail-message "agent_raw.py is missing @function_tool decorators."; exit 1; }

grep -q "Runner.run" /workspace/exercise/agent_raw.py \
    || { fail-message "agent_raw.py is missing Runner.run() call."; exit 1; }

echo "Check passed!"
